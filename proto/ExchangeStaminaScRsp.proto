syntax = "proto3";

import "ItemCost.proto";

message ExchangeStaminaScRsp {
  repeated ItemCost item_cost_list = 13;
  uint32 stamina_add = 2;
  uint32 exchange_times = 3;
  uint32 retcode = 11;
  int64 last_recover_time = 10;
}